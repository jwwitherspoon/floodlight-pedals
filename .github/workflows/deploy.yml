name: Deploy
run-name: Deploying to prod 🚀
on:
  push:
    branches:
      - 'master'
jobs:
  deploy:
    runs-on: 'ubuntu-latest'
    steps:
      - name: Install Node
        uses: actions/setup-node@v3
        with:
          node-version: 18
      # - run: cd ..
      - run: find ~/ -name package.json
      - run: pwd
      - name: Deploy to Prod
        run: npm run deploy
